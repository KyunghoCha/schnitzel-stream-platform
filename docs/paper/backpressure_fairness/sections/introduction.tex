\section{Introduction}
Queue overflow policy is often treated as a purely operational choice.
In fairness-sensitive pipelines, the same policy can produce unequal harm across groups,
even when average throughput remains similar.

This work asks: how should we evaluate backpressure policies when both reliability and fairness matter?
We present a reproducible benchmark protocol and report policy trade-offs with fixed statistical rules.
