# Packaging

## English

Status
------

Docker packaging is implemented under `Dockerfile` and entrypoint script.

Code Mapping
------------

- Dockerfile: `Dockerfile`
- Entrypoint script: Direct python module usage (see Dockerfile)

## 한국어

상태
----

Docker 패키징은 `Dockerfile`과 entrypoint 스크립트에 구현됨.

코드 매핑
---------

- Dockerfile: `Dockerfile`
- 엔트리 스크립트: Python 모듈 직접 실행 (Dockerfile 참조)
